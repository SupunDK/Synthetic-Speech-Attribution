The neural network has implemented using two codes.
1.part_1 for the noiseless neural network classifier
2.part_2 as the denoising algorithm.

In the part_2 of the code, user must first run denoising.ipynb to generate noise-free audio files and thereafter part_1 code to classify the signals.